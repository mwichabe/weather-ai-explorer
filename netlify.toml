[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/weather-proxy/*"
  to = "https://api.weather-ai.co/:splat"
  status = 200
  force = true

# SPA fallback — all unmatched routes serve index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
